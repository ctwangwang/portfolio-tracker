{
  "name": "portfolio-tracker-complete",
  "version": "2.0.0",
  "description": "Complete portfolio tracker with proxy server for real-time stock data",
  "scripts": {
    "install:proxy": "cd stock-proxy-server && npm install",
    "install:app": "cd portfolio-tracker && npm install", 
    "install:all": "npm run install:proxy && npm run install:app",
    "start:proxy": "cd stock-proxy-server && npm start",
    "start:app": "cd portfolio-tracker && npm start",
    "dev": "concurrently \"npm run start:proxy\" \"npm run start:app\"",
    "start": "echo 'Run npm run dev to start both applications'"
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  },
  "keywords": ["portfolio", "stocks", "real-time", "proxy-server", "react", "typescript"],
  "license": "MIT"
}
